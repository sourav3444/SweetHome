<html>
        <head>
            <%- include('../partials/head') %>
        </head>
    <body>
        <header>
            <%- include('../partials/_admin_head') %>
        </header>
        <main>
            <center>
                <% if(wlcmsg) { %>
                    <h6 class="text-success"><%= wlcmsg %></h4>
                <% } %>
                <div class="jumbotron">
                    <% if(wlcmsg) { %>
                    <h1 class="display-4">Hello <%= wlcmsg %> 
                        <h6 class="text-success">You are Welcome </h4>
                    <% } %></h1>
                    <p class="lead">This is a simple User page unit, a simple real estate business portal.</p>
                    <hr class="my-4">
                    <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
                    <p class="lead">
                      <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
                    </p>
                  </div>
            </center>
        </main>
        
        <footer>
            <%- include('../partials/footer') %>
        </footer>
    </body>
</html>